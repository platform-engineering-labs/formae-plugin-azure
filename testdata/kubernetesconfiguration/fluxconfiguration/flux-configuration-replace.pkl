/*
 * © 2025 Platform Engineering Labs Inc.
 *
 * SPDX-License-Identifier: FSL-1.1-ALv2
 */

amends "@formae/forma.pkl"
import "@formae/formae.pkl"

import "@azure/azure.pkl"

import "@azure/resources/resourcegroup.pkl"
import "@azure/containerservice/managedcluster.pkl"
import "@azure/kubernetesconfiguration/extension.pkl"
import "@azure/kubernetesconfiguration/fluxconfiguration.pkl"

local stackName = "plugin-sdk-test-flux-stack"
local testRunID = read("env:FORMAE_TEST_RUN_ID")

local rg = new resourcegroup.ResourceGroup {
  label = "flux-test-rg"
  name = "formae-plugin-sdk-test-flux-rg-\(testRunID)"
  location = "eastus"
}

local cluster = new managedcluster.ManagedCluster {
  label = "flux-test-aks"
  name = "fpsdt-flux-aks-\(testRunID)"
  location = "eastus"
  resourceGroupName = rg.res.name
  sku = new managedcluster.ManagedClusterSKU {
    name = "Base"
    tier = "Free"
  }
  identity = new managedcluster.ManagedClusterIdentity {
    type = "SystemAssigned"
  }
  dnsPrefix = "fpsdt-flux-aks-\(testRunID)"
  agentPoolProfiles = new Listing {
    new managedcluster.AgentPoolProfile {
      name = "nodepool1"
      count = 1
      vmSize = "Standard_D2s_v4"
      mode = "System"
      osType = "Linux"
    }
  }
}

local fluxExt = new extension.Extension {
  label = "flux-ext"
  name = "flux"
  resourceGroupName = rg.res.name
  clusterName = cluster.res.name
  extensionType = "microsoft.flux"
  autoUpgradeMinorVersion = true
}

forma {
  new formae.Stack {
    label = stackName
    description = "Plugin SDK test for Azure Flux Configuration"
  }

  new formae.Target {
    label = "azure-target"
    config = new azure.Config {
      subscriptionId = "ddfe7ebf-ba62-442b-b915-24f71b04e63b"
    }
  }

  rg

  cluster

  fluxExt

  // Replace: Change name (createOnly field triggers delete+create)
  new fluxconfiguration.FluxConfiguration {
    label = "plugin-sdk-test-flux-config"
    name = "flux-system-replaced"
    resourceGroupName = rg.res.name
    clusterName = cluster.res.name
    sourceKind = "GitRepository"
    namespace = "flux-system"
    scope = "cluster"
    gitRepository = new fluxconfiguration.GitRepository {
      url = "https://github.com/Azure/gitops-flux2-kustomize-helm-mt"
      repositoryRef = new fluxconfiguration.RepositoryRef {
        branch = "main"
      }
      syncIntervalInSeconds = 120
    }
    kustomizations = new Mapping {
      ["infra"] = new fluxconfiguration.Kustomization {
        path = "./infrastructure"
        prune = true
      }
    }
  }
}
